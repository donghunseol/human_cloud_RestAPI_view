# ??(tomcat) ?!
server:
  servlet:
    encoding:
      charset: utf-8
      force: true
    session:
      timeout: 30m
  port: 8080

# DB ??
spring:
  mustache:
    servlet:
      expose-session-attributes: true
      expose-request-attributes: true # mustache ???? session? request? ??? ????? ???.
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test;MODE=MySQL # MODE=MySQL ? ??(DB Dialect) ??
    username: sa
    password:
  # ?? URL ??? ?
  h2:
    console:
      enabled: true
#  sql:
 #   init:
  #    data-locations:
   #     - classpath:db/data.sql # ??? ??? ?? -> ??? ?? ? (???? ????? ???, ????? -> ???? -? ???? ???. YAML ????!)
  jpa:
    hibernate:
      ddl-auto: create # ddl - CREATE DROP ALTER TRUNCATE COMMENT -> none?? ?? ? ???? ?? ?? ??
    show-sql: true
    properties:
      hibernate:
        format_sql: true # sql ? ???? ??? ?? ??
    defer-datasource-initialization: true # sql ? table ?? ??? ?? ?? ??? ?? ??